# Orchestration Log — Reviewer

**Timestamp:** 2026-04-15T12:00:00Z  
**Agent:** Independent Reviewer  
**Task:** Pre-implementation AC review of issue #243 "feat: Add image support to slides"  
**Spawn trigger:** Maurice requested parallel team review before implementation begins  
**Output written:** `.squad/decisions/inbox/reviewer-issue-243-acs.md`

---

## What the agent did

Audited every acceptance criterion in issue #243 for completeness, testability, and internal consistency. Verified each AC against the current codebase state. Identified ambiguities that would force the implementer to invent behavior, and missing ACs that leave feature behavior untested.

## Key findings

### Ambiguities (implementer would have to guess)

| # | Severity | Finding |
|---|----------|---------|
| A1 | 🔴 Blocker | Position semantics completely undefined — 3 defensible implementations of `Top` alone |
| A2 | 🔴 Blocker | HTML structure ambiguous — `<figure>/<figcaption>` vs `<img>+<p>` vs `<img>+<span>` |
| A3 | 🔴 Blocker | `Src` path resolution unspecified — verbatim or prefixed; affects both rendering and file placement |
| A4 | ⚠️ Must Fix | AC 7 references non-existent "METR placeholder callout" — no such callout in current source; must name exact slide by Id |
| A5 | ⚠️ Should Fix | Alt validation (`ArgumentException`) conflicts with team "Errors Are Values" principle |
| A6 | ⚠️ Should Fix | CSS values underspecified ("small margin", "smaller font") — violates design token convention |

### Missing Acceptance Criteria

| # | Severity | Finding |
|---|----------|---------|
| M1 | 🔴 Blocker | No ACs for position-specific rendering — existing AC 8 only tests image presence, not placement |
| M2 | ⚠️ | No AC for `Caption=null` (no figcaption rendered) |
| M3 | ⚠️ | No AC requiring referenced image files exist in `wwwroot/` |
| M4 | ⚠️ | No E2E test requirement (team decision: user journeys must have E2E tests) |
| M5 | ⚠️ | No test project creation requirement in DoD |
| M6 | ⚠️ | No behavior specified for Position + absent neighbor (e.g., Top on slide with no Subtitle) |

## Outcome

**Verdict: Do not start implementation.** 6 blockers (3 ambiguities + 3 missing ACs) identified. 4 additional must-fix/should-fix items. Issue must be substantially amended before a line of code is written.
