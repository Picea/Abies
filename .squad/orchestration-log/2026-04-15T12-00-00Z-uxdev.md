# Orchestration Log — UI/UX Expert

**Timestamp:** 2026-04-15T12:00:00Z  
**Agent:** UI/UX Expert  
**Task:** Pre-implementation UX and accessibility review of issue #243 "feat: Add image support to slides"  
**Spawn trigger:** Maurice requested parallel team review before implementation begins  
**Output written:** `.squad/decisions/inbox/uxdev-issue-243-ux.md`

---

## What the agent did

Reviewed issue #243 from a UX and accessibility perspective in the context of a conference presentation app with slides projected full-screen. Applied WCAG 2.2, HTML semantics, and presentation UX patterns. Evaluated the proposed API from the slide author's DX perspective.

## Key findings

| # | Severity | Finding |
|---|----------|---------|
| 1 | 🔴 Must Fix | Alt text enforcement throws on empty string — violates WCAG 1.1.1 which **requires** `alt=""` for decorative images; trains authors to write fake alt text |
| 2 | 🔴 Must Fix | "figcaption-style element" will produce non-semantic HTML (`p`/`div`); must use `<figure>` + `<figcaption>` for screen reader caption association |
| 3 | 💡 Suggestion | `Inline` enum value is misleading (implies CSS inline); rename to `AfterContent` or `BelowPoints` |
| 4 | 💡 Suggestion | `Top`/`AfterContent`/`Bottom` positions are sound for a text-flow column layout; good design overall |
| 5 | 💡 Suggestion | DX improvement: discriminated union `AltText.Descriptive(string)` / `AltText.Decorative` makes illegal states unrepresentable and produces correct HTML for both cases |

## Outcome

2 must-fix items found. Both require design decisions before implementation: (1) AltText type design, (2) HTML structure specification. 3 suggestions submitted for team consideration.
