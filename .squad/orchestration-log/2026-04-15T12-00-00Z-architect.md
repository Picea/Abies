# Orchestration Log — Architect

**Timestamp:** 2026-04-15T12:00:00Z  
**Agent:** Architect  
**Task:** Pre-implementation design review of issue #243 "feat: Add image support to slides"  
**Spawn trigger:** Maurice requested parallel team review before implementation begins  
**Output written:** `.squad/decisions/inbox/architect-issue-243-design.md`

---

## What the agent did

Reviewed the issue #243 API sketch against the running codebase (`Picea.Abies.Presentation/Program.cs`). Applied Realist + Critic lenses (no Dreamer cycle — design already proposed by author). Compared the proposed `Slide` record against the actual implementation. Examined `ViewSlide` branching logic for `MarkdownBody` vs `Points` mode.

## Key findings

| # | Severity | Finding |
|---|----------|---------|
| 1 | 🔴 Blocker | `MarkdownBody` field is absent from the proposed record — present in current code and used on active slides |
| 2 | 🔴 Blocker | `ImagePosition` semantics undefined for `MarkdownBody` slides (mutually exclusive branch with `Points`) |
| 3 | ⚠️ Design | `Inline` name is misleading (CSS `inline` vs vertical-position anchor) — recommend `AfterContent` |
| 4 | ⚠️ Design | `SlideImage? Image = null` forecloses gallery migration — recommend `IReadOnlyList<SlideImage> Images = []` |
| 5 | ⚠️ Design | `ArgumentException` for empty Alt conflicts with "Errors Are Values" team principle |
| 6 | ℹ️ Info | Missing Width/Height HTML attributes — CLS risk on image-containing slides |
| 7 | ℹ️ Info | "figcaption-style element" ambiguous — should specify `<figure>/<figcaption>` |
| 8 | ℹ️ Info | `Src` path validation is convention-only, not enforced by type |

## Outcome

2 hard blockers identified. Issue must be amended before implementation. Recommendations for 3 design improvements submitted to inbox.
