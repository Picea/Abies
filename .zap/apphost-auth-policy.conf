# ZAP baseline policy for authenticated Conduit scanning.
# Format: <alert-id> <IGNORE|WARN|FAIL>

# Missing anti-CSRF tokens - API uses token-based auth, not cookie forms.
10202 IGNORE

# Timestamp disclosure / cache-like informational signals often noisy in API responses.
10096 WARN

# Keep all high-impact findings at default fail behavior.
