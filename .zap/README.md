# ZAP Profiles

This directory stores versioned OWASP ZAP scan policy and target profile files.

- `apphost-auth-policy.conf`: alert policy overrides for authenticated baseline scans.
- `apphost-auth-targets.txt`: authenticated endpoint list used by CI and local scripts.

Keep this file set minimal, reviewable, and security-owned.
